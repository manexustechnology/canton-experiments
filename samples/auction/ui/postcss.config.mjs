/**
 * Tailwind v4 via PostCSS. No tailwind.config.js — config is CSS-first
 * in app/tenzro-tokens.css (the vendored Tenzro token layer).
 */
export default {
  plugins: ['@tailwindcss/postcss'],
};
